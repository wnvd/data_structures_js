// there are more then one way of implementing stack
let stack = [];
stack.push('google');
stack.push('youtube');
stack.push('facebook');
console.log(stack);
stack.pop();
console.log(stack);
stack.unshift('download a file'); // insert at begining
console.log(stack);
stack.shift(); // removing begining
console.log(stack);
// adding and removing at begining  is not a good thing
